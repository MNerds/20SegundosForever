using TMPro;
using UnityEngine;

public class RewardTicketForever : MonoBehaviour
{
    [SerializeField] private TextMeshProUGUI ticketCount;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

}
